ROLE: Senior knowledge-engineer (reconciliation & conflict modeling).

CONTEXT: <paste SHARED CONTEXT BLOCK>
Phases 1-3 exist: normalized, embedded facts with provenance.

OBJECTIVE: Stage 6: pairwise reconciliation producing fact_relations with human-readable reasoning.
Must yield the assignment's relation types: corroborates, contradicts, contextual_variance, needs_review.

DELIVERABLES:
1. api/pipeline/reconcile.py:
   - CANDIDATES: (a) exact groups on (entity_canon, attribute_canon) across different documents;
     (b) semantic sweep: for each new fact, pgvector cosine > 0.86 against facts of other documents
     with different attribute_canon (catches "net debt" vs "total debt less cash").
   - RULE ENGINE (ordered, deterministic, each rule id logged):
     R1 both confidence < 0.5 -> needs_review ("low-confidence extraction on both sides").
     R2 context diff detected (period != period, or scope != scope, or entity_canon != entity_canon,
        or norm_unit/currency differ WITHOUT convertible rate) -> contextual_variance;
        explanation lists exactly which context axes differ, e.g. "periods differ: FY2022 vs FY2023".
     R3 convertible units (currency with static FX constant table FX_RATES, or pct vs fraction):
        convert, then compare.
     R4 same context: rel_delta = abs(a-b)/max(abs(a),abs(b)); <= TOL (0.01) -> corroborates
        ("match within 1% tolerance after unit normalization"); else contradicts
        ("same entity/period/scope, values differ by X%, no transformation explains it").
     R5 non-numeric facts: string equality after canon (addresses: lowercase, postal-code match,
        rapidfuzz > 90) -> corroborates; direct opposition verbs (resigned/active via small verb-set map)
        -> contradicts; else needs_review.
   - explanation: full sentence built from rule outputs; rules_applied jsonb = ["R2:period", "R3:FX:INR->USD", ...].
   - INCREMENTAL: only pairs where at least one fact was created in this run.
   - Write fact_relations + audit(action='reconcile').
2. Endpoints: GET /relations?type=&document_id= ; GET /facts/{id} now embeds its relations with the
   counterpart fact's quote/page/doc filename.
3. run_pipeline stage 'reconcile' then 'done'.

CONSTRAINTS: no LLM in the decision path (Gemini may ONLY be used optionally to phrase the final
explanation sentence AFTER the rule decision — keep rule ids as source of truth); tolerance and FX table
are documented constants; every relation must be reproducible from rules_applied alone.

ACCEPTANCE CRITERIA:
- Seeding the 3 starter PDFs produces >=1 corroborates, >=1 contradicts or contextual_variance;
  each relation row's explanation cites the differing context axes or tolerance math.
- Re-running pipeline on a 4th PDF creates only new pairs (check audit counts).

COMMIT MESSAGE: "feat(reconcile): deterministic rule engine for corroboration/contradiction/contextual variance with audited reasoning"