ROLE: Senior API engineer (retrieval & grounded generation).

CONTEXT: <paste SHARED CONTEXT BLOCK>
Phases 1-4 exist: full knowledge layer in Postgres.

OBJECTIVE: Expose everything the frontend needs, including a chatbot backend that answers ONLY from
stored facts with clickable citations (Fact-Centric RAG: retrieve facts, not raw chunks).

DELIVERABLES:
1. GET /matrix -> {entities: [...], attributes: [...], cells: {entity|attribute: [fact_ids]}} with per-cell
   badge = worst relation type among the fact's relations (contradicts > contextual_variance > corroborates).
2. GET /facts?document_id=&entity_canon=&attribute_canon=&q= (q => pgvector cosine top-k on facts.embedding).
3. GET /pages/{document_id}/{page} -> 302 redirect to R2 public PNG URL (plus page w/h from parse meta).
4. GET /audit?fact_id= -> lineage rows for one fact (extract call, ocr call, reconcile decisions).
5. POST /chat {message, history: []} — grounded RAG:
   a. embed message (Gemini); retrieve top-8 facts by cosine + top-5 by SQL entity/attribute token match;
      merge, dedupe, keep facts with confidence > 0.4.
   b. context block = per fact: "[F{id}] {entity} {attribute} = {raw_value} ({period}, {scope}) — doc {filename} p.{page}: \"{quote}\"".
   c. Groq prompt: answer in markdown using ONLY the context; cite every claim as [F{id}];
      if context insufficient say exactly "The knowledge layer has no grounded fact for this." and stop.
   d. parse [F{id}] tokens -> response {answer, citations: [{fact_id, page, filename, quote}]}.
   e. audit(action='chat', meta={retrieved_ids, answer_len}).
6. Error/limit guards: max 40 retrieved facts, 60s timeout wrapper, uniform {"error"} shape.

CONSTRAINTS: chat must never quote text that is not in a stored fact quote; citations array must equal
the set of ids present in answer; empty retrieval => refusal message (no hallucination path).

ACCEPTANCE CRITERIA:
- curl /matrix returns grid for seeded docs; /facts/{id} shows quote+page+bbox+relations.
- curl /chat "What was Acme's revenue in FY2023?" returns markdown answer with >=1 citation whose
  fact_id resolves via GET /facts/{id} to a matching quote.
- A question about absent info returns the exact refusal sentence.

COMMIT MESSAGE: "feat(api): matrix/evidence/audit endpoints + grounded fact-centric chat RAG with citations"