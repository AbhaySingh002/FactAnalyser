ROLE: Senior frontend engineer + product designer (fintech due-diligence tools).

CONTEXT: <paste SHARED CONTEXT BLOCK>
Backend complete (Phase 5 contract): /documents /jobs /facts /facts/{id} /matrix /relations /pages /audit /chat.

OBJECTIVE: Build the Vercel-deployed UI: landing page, upload dashboard, Fact Matrix, click-to-source
Evidence viewer with PDF page + bbox highlight, grounded Chat with citation chips, relation visualization,
and a 4-cases showcase. Dark, dense, professional (Linear/Hebbia feel), shadcn/ui everywhere.

SETUP: Next.js 14 App Router, TypeScript, Tailwind, `npx shadcn@latest init` (dark default, zinc base).
Add components: button, card, table, badge, sheet, dialog, tabs, skeleton, progress, scroll-area,
separator, tooltip, textarea, avatar, accordion, alert, sonner, dropdown-menu, input.
lib/api.ts typed client for the contract above; hooks/useJob.ts polling (2s) hook.

PAGES & SPEC:
1. `/` LANDING: hero (headline "Every fact. Grounded. Every conflict. Explained.", subline, CTA Button
   "Open Workspace" + secondary "View Architecture"), 3-step Card row (Parse → Extract & Ground → Reconcile),
   architecture Card with monospace pipeline diagram, footer. Use Badge accents, subtle grid background.
2. `/app` DASHBOARD: drag-drop upload Card (Input file + Button), documents Table (filename, pages,
   status Badge, created), per-row Progress driven by useJob(stage), Sonner toast on done/failed,
   Button "Open Matrix".
3. `/app/matrix`: shadcn Table, sticky first column = entities, headers = attributes; each cell shows
   value + relation dot Badge (emerald=corroborates, rose=contradicts, amber=contextual_variance,
   zinc=needs_review) with Tooltip explanation; cell click opens Evidence Sheet.
4. EVIDENCE SHEET (right Sheet, reusable component <EvidenceSheet factId>):
   - Header: entity · attribute · value + confidence Badge + model/prompt_ver monospace line.
   - Alert with verbatim quote.
   - Accordion "Cross-document relations": each relation with type Badge, explanation, rules_applied chips.
   - Accordion "Lineage": /audit rows (ocr provider, extract model, reconcile rules).
   - PAGE VIEWER: <img src=/pages/{doc}/{page}> inside ScrollArea with zoom Buttons (+/-/reset);
     absolute-positioned highlight div computed from bbox scaled by (renderedWidth / pageWidth),
     2px emerald border + 15% emerald fill; caption "Page {n} · {filename}".
5. `/app/chat`: message list (Avatar bot/user), assistant messages rendered with react-markdown;
   citation tokens become Badge chips "[F12]" (variant outline, monospace) — click opens EvidenceSheet;
   composer = Textarea + Button send, Enter-to-send, Skeleton bubbles while awaiting;
   empty-state Card with 3 suggested questions Buttons.
6. `/app/graph`: simple visualization — facts as nodes, fact_relations as edges colored by relation type;
   implement with plain SVG (circle + line + label) or reactflow if trivial; legend Card; node click -> EvidenceSheet.
7. `/app/cases`: Tabs = the four required cases; each tab lists matching relations/facts fetched from
   /relations (types) and confidence<0.5 facts (failure tab); each item Button "Show evidence" -> Sheet.
DESIGN RULES: consistent 8pt spacing; monospace (font-mono) for values, pages, ids; semantic colors ONLY
for relation states; every async view has Skeleton; every error shows Alert + retry Button; no dead buttons.

DEPLOY: Vercel project, env NEXT_PUBLIC_API_URL=https://{render}.onrender.com; backend CORS set to the
Vercel domain; verify cold-start toast handling (first call may take ~60s: show Progress "waking backend").

ACCEPTANCE CRITERIA:
- Deployed URL: upload PDF from browser -> job progresses -> Matrix populated.
- Click a contradicts cell -> Sheet shows both quotes, explanation, and the highlighted page region.
- Chat answer renders citation chips; clicking a chip opens the exact evidence.
- /app/cases shows all four tabs with at least one item each (seed data).
- Lighthouse-free check: no console errors, dark theme consistent.

COMMIT MESSAGE: "feat(web): landing, matrix, bbox evidence viewer, grounded chat UI, relation graph, 4-case showcase (shadcn/ui)"